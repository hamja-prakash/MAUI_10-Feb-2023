<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MAUISampleDemo.View.Device_APPInfoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolbar="clr-namespace:MAUISampleDemo.View.FieldModifier"
    Title="Device_APPInfoPage">
    <ScrollView>
        <VerticalStackLayout
            Padding="0"
            Background="{AppThemeBinding Light={StaticResource LightBackground},
                                         Dark={StaticResource DarkBackground}}"
            Spacing="10">
            <toolbar:CommonToolbarView
                x:Name="CommonToolbar"
                x:FieldModifier="Private"
                HorizontalOptions="FillAndExpand" />
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="Device Informtaion"
                TextColor="{StaticResource BaseLabel}" />
            <Label x:Name="LblDeviceInfo" FontSize="18" />
            <Label
                FontAttributes="Bold"
                FontSize="22"
                Text="App Informtaion"
                TextColor="{StaticResource BaseLabel}" />
            <Label
                x:Name="LblAppInfo"
                FontSize="18"
                TextColor="{StaticResource BaseLabel}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>